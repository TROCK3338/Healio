<!DOCTYPE html>
<html>
  <head>
  <title>Healio</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= favicon_link_tag 'favicon.png' %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload", "media": "all" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

  <!-- Font Awesome -->
  <%= tag.link href: "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css", rel: "stylesheet" %>

  <!-- Chart.js + Chartkick + Luxon Date Adapter -->
  <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/chart.js@4.5.0/dist/chart.umd.js", crossorigin: "anonymous" %>
  <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/luxon@3.4.4/build/global/luxon.min.js", crossorigin: "anonymous" %>
  <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/chartjs-adapter-luxon@1.3.1/dist/chartjs-adapter-luxon.umd.js", crossorigin: "anonymous" %>
  <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/chartkick@5.0.1/dist/chartkick.js", crossorigin: "anonymous" %>
</head>

  <body class="bg-gray-100 text-gray-900">
    <!-- Floating Navbar -->
    <nav class="fixed top-0 left-0 w-full z-50 bg-white/90 backdrop-blur shadow-md px-6 py-3 flex justify-between items-center">
  <!-- Brand -->
  <div class="text-xl font-bold text-blue-700">
    Healio
  </div>

  <!-- Search Bar -->
  <div class="hidden sm:block w-full max-w-md mx-6">
    <input type="text" placeholder="Search patients..." class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring focus:border-blue-400" />
  </div>

  <!-- Icons -->
  <div class="flex items-center gap-4 text-gray-600 text-xl">
    <!-- Add Patient -->
    <%= link_to new_patient_path, title: "Add Patient", class: "hover:text-blue-600 transition" do %>
      <i class="fas fa-user-plus"></i>
    <% end %>

    <!-- Logout -->
    <%= link_to destroy_user_session_path, method: :delete, data: { confirm: "Logout?" }, title: "Logout", class: "hover:text-red-600 transition" do %>
      <i class="fas fa-sign-out-alt"></i>
    <% end %>
  </div>
</nav>

    <main class="pt-24 px-4">
      <%= yield %>
    </main>
  </body>
</html>